Process trumbowyg.scss twice, with alternate boolean values for the embedded variable

$light-theme

Default value: true

May need to manually edit the .scss file to change the value

trumbowyg.scss also uses a variable

$leftwards

which may be set false to generate rtl-compatible styles
